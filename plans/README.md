# Implementation plans

Generated by the improve audit on 2026-09-21. Plans target commit `f4af627`.
Execute them in order unless a maintainer chooses a different order. Each
executor must read the full plan, honor its STOP conditions, and update its
status row after completion.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Remove invalid package entry points | P1 | S | - | DONE |
| 002 | Harden notification task ownership | P1 | M | - | DONE |
| 003 | Make probe scan windows accurate | P2 | S | - | DONE |
| 004 | Reject malformed advertisements safely | P2 | S | - | DONE |
| 005 | Publish package typing metadata | P2 | S | 001 | DONE |
| 006 | Add Zendure app source contract fixture | P2 | M | - | DONE |
| 007 | Align client with Zendure app handshake | P1 | M | 006 | DONE |
| 008 | Add standalone library test script | P2 | M | 007 | DONE |

## Dependency notes

- Plan 005 depends on Plan 001 only because both change package metadata and
  should not be edited concurrently. It does not depend on the behavior of
  Plan 001.
- Plans 001-004 touch separate primary implementation concerns and can be
  executed in parallel if the executor preserves the listed file scopes.

## Findings considered and rejected

- Dependency vulnerability audit: not ranked because `uv run pip audit`
  could not start: the environment has no `pip` executable. Re-run with a
  working `pip-audit` or equivalent tool before release.
- README accuracy: audited and corrected in the working tree before this
  plan set; no additional README plan is warranted.
- Probe redaction: existing tests cover device ID, product key, pack serial,
  nested values, and binary payloads in `tests/test_probe.py`; no new plan
  unless new capture fields are introduced.
- Full Home Assistant integration: explicitly out of scope in `AGENTS.md` and
  the repository has no `custom_components/` surface.

## Audit scope

Audited the Python package, developer probe, tests, package metadata, lockfile,
README, AGENTS.md, CI workflow, research note, and recent git history. The
audit did not inspect live Bluetooth hardware behavior, a real ESPHome proxy,
or upstream dependency source beyond the installed APIs used by the probe.
